# Logs
# Ignore log files, which are typically generated during runtime and can become large.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
# These are managed by package managers and can be reinstalled from package.json.
# Including them in the repo would bloat its size unnecessarily.
node_modules
jspm_packages

# Environment variables
# Never commit environment variables. They often contain sensitive credentials
# and should be managed separately. Create a .env.example file instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Build output
# These are generated files from your source code. They should be generated
# on the server or during a build process, not stored in the repo.
/dist
/build
.next
/out

# Coverage directory
# Generated by testing frameworks to report code coverage.
/coverage
/coverage.*

# Production files
# Files specific to a production environment.
/build
.pnp.*

# System files
# Ignore OS-specific files that have no relevance to the project itself.
.DS_Store
Thumbs.db

# Optional files
# Temporary files from various editors or tools.
.idea
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
